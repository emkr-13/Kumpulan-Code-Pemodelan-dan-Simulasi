#include <iostream>
#include <vector>

// Function to plot a point in 3D space
void plot3(double x, double y, double z) {
    // Assuming a simple console-based representation
    // You may need a more sophisticated approach for graphical libraries
    std::cout << "x: " << x << ", y: " << y << ", z: " << z << std::endl;
}

int main() {
    // Example usage of plot3 function
    plot3(1.0, 2.0, 3.0);

    return 0;
}
